server:
    port: 10001
    servlet:
        context-path: /usermodule
spring:
    profiles:
        active: dev
    jpa:
        hibernate:
            ddl-auto: update
        database: mysql
        show-sql: true
        database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    servlet:
        multipart:
            max-file-size: 200MB
            max-request-size: 200MB
    jackson:
        date-format: yyyy-MM-dd HH:mm:ss
        time-zone: GMT+8

